apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-dev-api
  labels:
    app: tt-dev-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tt-dev-api
  template:
    metadata:
      labels:
        app: tt-dev-api
    spec:
      containers:
      - name: ittyni
        image: khmammed/ittyni:api_dev
        imagePullPolicy: Always
        ports:
        - containerPort: 8060
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: tt-dev-api-service
  labels:
    app: tt-dev-api
  namespace: default
spec:
  ports:
  - port: 8060
    protocol: TCP
    targetPort: 8060
  selector:
    app: tt-dev-api