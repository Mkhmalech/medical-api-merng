import * as AM from '../controller/am'
export const AMResolver = {
    updateAccountName : AM.updateAccountName,
    updateAccountType : AM.updateAccountType,
    updateAccountStartDate : AM.updateAccountStartDate,
    updateContact : AM.updateContact,
    fetchAccountData : AM.fetchAccountData,
};