import * as appointment from '../controller/appointement'

export const Appointement = {
    fetchAppointements : appointment.fetchAppointements,
    createAppointement : appointment.createAppointement
}