export const readAllPacks = async (args: any, {user, permissions, message}: any)=>{

    return "all packs"
}

export const createPackForLab = async (args: any, {user, permissions, message}: any)=>{

    return "PACK_CREATED_SUCCEFULLY"
}